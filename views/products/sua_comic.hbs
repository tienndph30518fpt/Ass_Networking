<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Comic</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
</head>
<body>
    <div class="container">
        <h1>Edit Comic</h1>
        <form action="/api/comics/edit/{{comic._id}}" method="post" enctype="multipart/form-data">
            <!-- Title -->
            <div class="form-group">
                <label for="title">Title</label>
                <input class="form-control" type="text" name="title" id="title" value="{{comic.title}}" required>
            </div>

            <!-- Description -->
            <div class="form-group">
                <label for="description">Description</label>
                <textarea class="form-control" name="description" id="description" rows="3" required>{{comic.description}}</textarea>
            </div>

            <!-- Author -->
            <div class="form-group">
                <label for="author">Author</label>
                <input class="form-control" type="text" name="author" id="author" value="{{comic.author}}" required>
            </div>

            <!-- Publish Year -->
            <div class="form-group">
                <label for="publishYear">Publish Year</label>
                <input class="form-control" type="number" name="publishYear" id="publishYear" value="{{comic.publishYear}}" required>
            </div>

            <!-- Images -->
            <div class="form-group">
                <label for="images">Images</label>
                <input class="form-control" type="file" name="images" id="images">
                <img src="{{comic.images}}" alt="Comic Image" width="100">
            </div>

            <!-- Content Image (PDF) -->
            <div class="form-group">
                <label for="content_img">Content Image (PDF)</label>
                <input class="form-control" type="file" name="content_img" id="content_img">
                <!-- Hiển thị tên của file PDF hiện tại nếu muốn -->
                <!-- Ví dụ: <p>{{comic.content_img}}</p> -->
            </div>

            <!-- Button -->
            <button type="submit" class="btn btn-primary">Save</button>
            <a href="/api/comics" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>
